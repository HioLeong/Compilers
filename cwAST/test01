fdef sum(i: int, j:int) : int {
     return i + j;
}

{

  s1 : int = 1;
  s2 : float = 2;
  b : bool;

  if (s1 < s2 || s1 == s2) {
     b  =  s1 + s2 / (s1 + s2) >= 30;
  } else {
     /* do something else */
  }
}