package ast.expression;

import ast.Node;

/**
 * No implemented functionality, so also here an interface could be an
 * alternative to an abstract class.
 */
public abstract class ExprNode extends Node {
	
}
